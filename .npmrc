# Required for pnpm-monorepo + React Native / Expo Gradle resolution.
# Without hoisted linking, EAS/Gradle fails with:
#   Included build '.../android/null' does not exist
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
